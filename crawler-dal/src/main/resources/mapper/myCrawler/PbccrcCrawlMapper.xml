<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.maiya.dal.dao.myCrawler.PbccrcCrawlDao">

    <select id="getPbccrcSite" resultType="PbccrcSite">
        SELECT * FROM crawl_pbccrc_site WHERE is_need_crawl=1
    </select>

</mapper>